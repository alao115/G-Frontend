export default class serviceGenerator {
  // create (data) {
  //   return http.post(`/${serviceName}/`, data)
  // }

  // getAll () {
  //   return http.$get(`/${serviceName}/`)
  // }

  // getOne (entityID) {
  //   return http.$get(`/${serviceName}/${entityID}`)
  // }

  // update ({ entityID, data }) {
  //   return http.patch(`/${serviceName}/${entityID}`, data)
  // }

  // delete (entityID) {
  //   return http.delete(`/${serviceName}/${entityID}`)
  // }

  // search (data) {
  //   return http.post(`/${serviceName}/search`, data)
  // }
  // query (projections = 'id') {
  //   return apollo.query({ query: gql`query { publications { ${projections} } }` })
  // }
}
